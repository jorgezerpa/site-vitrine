import { headerImage } from '../../assets';

export const styles = {
    Box: {backgroundImage:`url(${headerImage})`, backgroundPosition: 'center', backgroundSize:'cover', backgroundRepeat:'no-repeat'},

}